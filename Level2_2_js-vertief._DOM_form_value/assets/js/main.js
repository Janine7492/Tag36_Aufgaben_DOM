function values() {
    const name = document.getElementById("vorname").value;

    const lastName = document.getElementById("nachname").value;

    const land = document.getElementById("land").value;

    console.log("Full name: " + name + " " + lastName + ", Land: " + land);
}